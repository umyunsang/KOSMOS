T014 — bun run typecheck (UMMAYA official narrow scope: src/stubs/** + tests/unit/stubs/**)
$ tsc --noEmit -p tsconfig.typecheck.json
exit=0

Notes:
- The official UMMAYA typecheck (tsconfig.typecheck.json) excludes src/utils/** + src/ink/**
  per Epic #1633 P1 cleanup tracking. The newly created src/utils/sessionTitle.ts is
  byte-identical with CC and uses imports verified to resolve in T011.
- bun test (which compiles via Bun's transpiler, not tsc) confirms full TypeScript
  compatibility: 3/3 PASS in us1-test-output.txt.
- cli/print.ts:156 import resolves via Bun's module resolution (verified by ts test
  runner; full tsc check on print.ts hits unrelated pre-existing node_modules errors
  that exist on main commit 9d559b9).
