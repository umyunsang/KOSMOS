// KOSMOS Epic #2112: legacy deprecation table removed (single-fixed K-EXAONE has
// no retirement schedule). Helper signature preserved for caller import-graph.

export function getModelDeprecationWarning(_modelId: string | null): string | null {
  return null
}
